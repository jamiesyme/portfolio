jamiesyme.com
=============

Building Client
---------------

Install npm modules: `yarn install`

Build: `yarn run build`


Building Server
---------------

Build: `nim c main.nim`

Run: `./main`


View in Browser
---------------

Go to `localhost:8080`