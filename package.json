{
  "name": "jamiesyme.com",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Jamie Syme <jamie.d.syme@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build-client": "babel client -d tmp && browserify tmp/app.js -o build/bundle.js && rm -rf tmp",
    "build-server": "babel server -d build",
    "build": "babel client -d tmp && browserify tmp/app.js -o build/bundle.js && rm -rf tmp && babel server -d build",
    "start": "(cd build && node app.js)"
  },
  "babel": {
    "presets": [
      "react",
      "es2015"
    ]
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-react": "^6.16.0",
    "browserify": "^13.1.1"
  },
  "dependencies": {
    "express": "^4.14.1",
    "jquery": "^3.1.1",
    "normalize.css": "^5.0.0",
    "radium": "^0.18.1",
    "react": "^15.4.1",
    "react-autosize-textarea": "^0.3.6",
    "react-dom": "^15.4.1",
    "react-scroll": "^1.4.7",
    "underscore": "^1.8.3"
  }
}
